var processorService = require("../../src/service/ProcessorService");

//jest.mock("../../src/service/ProcessorService");

describe('ProcessorService', () => {
    it('process method should return success', async() => {
        // var processorRequestBo = { domain: 'finance',
        // interfaceName: 'Coda',
        // jobName: 'coda001',
        // fileName: 32 }

        // var result = await processorService.selectOperation(JSON.parse(processorRequestBo.stringify));
        // //var jobResource = JSON.parse(JSON.stringify(result)).jobResource;
        // console.log("***********",JSON.stringify(result));
    });
});